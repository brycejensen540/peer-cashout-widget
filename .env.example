# Peer Cash-out widget environment.
# Pick which Peer backend to talk to: 'staging' (default), 'preproduction', or 'production'.
# NOTE: all environments run against the SAME Base mainnet chain and Base USDC —
# the env only selects the Peer curator/indexer API. Funds are real USDC everywhere.
VITE_CASH_ENV=staging
